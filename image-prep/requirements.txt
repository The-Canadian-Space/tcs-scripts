Flask>=3.0,<4.0
gunicorn>=21.0,<24.0
Pillow>=10.0,<12.0
# C2PA provenance manifests on /header output (tcs-scripts#21). Pinned to the
# minor: the Python API has moved between minors before. Bump deliberately.
c2pa-python>=0.37.10,<0.38
